<div class="container">
  <h3 class="mt-3">Découvrez les associations qui recrutent !</h3>
  <div class="break"></div>
  <div class="row">
    <% @nonprofits.each do |nonprofit| %>
      <div class="col-xl-3 col-lg-4 col-md-6 col-sm-12">
      <div class="card-asso">
        <%= link_to nonprofit_path(nonprofit) do %>
          <div class="card-asso-img">
            <h5><%= nonprofit.name %></h5>
          </div>
        <% end %>
          <div class="card-asso-content">
            <p><%= nonprofit.mission %></p>
            <h6><i class="fa-solid fa-location-dot"></i><%= nonprofit.city %></h6>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<%= link_to 'New', new_nonprofit_path %>
