<div class="container">
  <h3 class="mt-3">Découvrez les associations qui recrutent !</h3>
  <div class="break mb-3"></div>
  <div class="row">
    <% @associations.each do |association| %>
      <div class="col-xl-3 col-lg-4 col-md-6 col-sm-12">
      <div class="card">
        <%= link_to association_path(association) do %>
          <div class="card-img">
            <h5><%= association.name %></h5>
          </div>
        <% end %>
          <div class="card-content">
            <p><%= association.mission %></p>
            <h6><i class="fa-solid fa-location-dot"></i><%= association.city %></h6>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<%= link_to 'New', new_association_path %>
