<div class="banner-offer" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/background.png);">
  <div class="container">
    <h1><%= @offer.title %></h1>
    <p><%= @offer.nonprofit.name %></p>
    <a class="btn btn-flat" href="#">Apply now</a>
  </div>
</div>
<div class="container">
  <%= @offer.start_date %>
  <%= @offer.duration %>
  <%= @offer.location %>
  <%= @offer.frequency %>
  <%= @offer.description %>
</div>

<%= simple_form_for [@offer, @candidature] do |f| %>
  <%= f.input :motivation, label: "Dites-nous en plus sur vos motivations à rejoindre #{@offer.nonprofit.name}" %>
  <%= f.submit %>
<% end %>
